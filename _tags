true : bin_annot, safe_string, package(bytes), package(angstrom), package(oUnit), package(mirage-types-lwt), package(astring), package(rresult)

<src> : include

<src/pl_*.ml> : \
	package(angstrom)

<test> : include

<test/*.{ml,mli}> : \
	package(oUnit), package(result)