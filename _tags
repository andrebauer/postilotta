true : bin_annot, safe_string, package(bytes), package(angstrom), package(oUnit)

<src> : include

<src/pl_pop3.ml> : \
	package(angstrom)

<test> : include

<test/*.{ml,mli}> : \
	package(oUnit), package(result)